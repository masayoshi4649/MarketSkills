# MCPデータ再調査（2026年8月14日更新版）

## 1. 位置付け

この文書は、更新された「市場データ収集」と「ニュース取得ツール」を2026年8月14日02:28～02:32 JSTを中心に再監査し、OP限月更新後の03:08～03:11 JSTにも再確認したうえで、次の4つのChatGPT用SKILLへどう組み込むべきかを整理した最新版です。

1. 日経225・TOPIXの現在時点基準予測と、単純方向・NT取引の提示
2. 最新下落率ランキングからの日本株復活可能性評価と出口戦略
3. 市場拡大が見込まれる日本株の中長期成長評価と出口戦略
4. 特定銘柄の株価シナリオ予測

[初回調査](MCPデータ調査.md)は更新前の挙動を残す履歴資料とし、本書を現在の設計根拠とします。実測値は機能・鮮度・欠損の確認例であり、現在の売買推奨ではありません。

再監査では注文、取消、銘柄登録、登録解除、口座・建玉操作を行っていません。

その後、`S:\GitHub\masayoshi4649\MarketDataCollector`と`S:\GitHub\masayoshi4649\kabus-controller`の実装を確認し、登録状態について訂正しました。MarketDataCollectorがKabusControllerへ送る要求はすべてGETです。KabusControllerの登録状態を変えるのは、`PUT /register`、`PUT /unregister`、`PUT /unregister/all`を実行した場合だけです。板、銘柄情報、規制、優先市場、信用プレミアム、銘柄コード解決等のGETは登録状態を変えません。

## 2. 結論

更新によって4つのSKILLに必要な主要データはほぼ揃いました。市場データ全体は6提供元のまま、データセットが94件から106件へ増え、KabusControllerが6件から18件になりました。ニュースは8ソースから17ソースへ増えました。

ただし、次の区別が必須です。

| 判定 | 確認するもの |
|---|---|
| 取得できるか | データセットが存在し、呼出しが成功するか |
| 対象時点が分かるか | 日付付き`source_at`、対象日、開示日時があるか |
| 現在の市場を表すか | 取得時刻だけでなく、現在セッションの価格・板か |
| 取引可能か | 両側板、スプレッド、出来高、取引最終日、規制が正常か |
| 状態を変更しないか | GETだけを使用し、`/register`、`/unregister`、`/unregister/all`を呼んでいないか |

今回の重要な判断は次のとおりです。

- 登録済み先物の板は秒～分単位で取得でき、現在時点基準の指数・NT分析に使えます。
- 最終約定時刻の古さと板スナップショットの取得遅延は別物です。低頻度商品を30秒だけで一律失格にしてはいけません。
- `kabus_ranking`は50件規模のランキング、出来高、売買代金、業種を返しますが、対象日を返しません。J-Quantsまたは225225.jpで日付を照合します。
- 任意現物板、規制、銘柄情報、信用プレミアム、resolverはGETであり、SKILLから安全に参照できます。現行MCPの`read_only:false`と`may_register_symbol:true`は実装実態と一致しないメタデータです。
- オプション登録は、次セッションに対する直前セッションの日経225先物終値を基準に、設定範囲を125円刻みでCall・Putの両方について構成します。プログラムがセッション途中から開始した場合は、起動時点の日経225先物値が基準です。
- 初回監査でオプション板が空だった原因は、SQ日に設定限月が`202608`のままだったためです。更新後は`202609`限の34契約すべてで両側板を取得できました。OPのJSON板は取得時点のライブスナップショットとして利用できますが、最終約定時刻と板取得時刻は分けて扱います。
- 為替は許可値11ペア中、実測で成功したのはUSD/JPYだけでした。時刻に日付がなく、別ソース照合が必要です。
- 新しいニュースソースはマクロ調査資料とJPX取引所イベントを強化しましたが、企業IR・TDnet・決算短信の網羅性は改善していません。
- `enabled=true`は「データが存在し、定常取得できる」という意味ではありません。新規9ソース中4ソースは記事0件でした。
- J-Quantsは引き続き日次・週次の公式データです。取得が現在でも、データがリアルタイムになるわけではありません。
- 現在価格と保存済みPERを混ぜず、現在値と同じ評価期のEPSから倍率を再計算する必要があります。

## 3. 更新差分

### 3.1 市場データ収集

KabusControllerに次の12データセットが追加されました。

| データセット | 取得内容 | 主用途 | 主な制約 |
|---|---|---|---|
| `kabus_ranking` | 値上がり・値下がり・出来高・売買代金・信用・33業種ランキング | 候補抽出、市場の広がり | 対象日なし、信用は週次 |
| `kabus_regulations` | 銘柄別規制 | 除外・執行警告 | 基準日時なし |
| `derivative_symbol_resolver` | 先物・OPコード解決 | 明示限月・権利行使価格の特定 | 解決成功と板取得可否は別 |
| `nt_pair_symbol_resolver` | ミニTOPIXと日経mini/microの同限月解決 | NT誤限月防止 | GETのみ。両脚を明示限月で検証 |
| `arbitrary_board_snapshot` | 任意の現物・先物・OP板 | 現在値、入口、スプレッド | GETのみ。取得不能時は別経路へ切替 |
| `option_chain_snapshot` | 登録済みOPのATM周辺チェーン | ボラティリティ面の補助 | 登録済み限定、建玉なし |
| `kabus_symbol_info` | 銘柄情報、決算期、値幅、時価総額等 | 単位、取引単位、制限値幅 | 単位検証が必要 |
| `kabus_primary_exchange` | 優先市場 | 市場コード確認 | GETのみ |
| `kabus_fx_snapshot` | 為替Bid・Ask・時刻 | USD/JPY地合い | 実測ではUSD/JPY以外失敗、日付なし |
| `kabus_margin_premium` | 一般信用・デイトレ信用プレミアム | ショート実行コスト | 基準日の解釈が必要 |
| `kabus_api_soft_limits` | 1注文当たり上限、バージョン | 誤った枚数解釈の防止 | 登録残枠や証拠金ではない |
| `kabus_api_capacity` | Controller既知の登録件数と上限 | 容量警告 | 正確な残枠ではない |

KabusController系には、`collected_at`、`upstream_fetched`、`source_at`、`age_seconds`、`market_state`、`is_stale`、`stale_reason`、`request_parameters`、`contract_month`、`read_only`、`may_register_symbol`などのメタデータも追加されました。

重要な解釈は次のとおりです。

- `is_stale:null`は「新鮮」ではなく「判定不能」です。
- `market_state:"unknown"`は開場中を意味しません。
- 集約データの`age_seconds`は、分析対象外の最古行に引っ張られる場合があります。対象各行を確認します。
- 現行MCPが情報GETへ返す`read_only:false`、`may_register_symbol:true`、`side_effect_notice`は、確認した実装契約と一致しません。SKILLではGETを読取専用として扱い、MCP側ではメタデータの訂正が必要です。

### 3.2 ニュース取得ツール

有効ソースは17件になりました。新規とみられる9ソースの実測状況は次のとおりです。

| ID | ソース | 保存記事の実測 | 適した用途 |
|---:|---|---:|---|
| 9 | 大和総研 経済分析 | 10件 | 金利、為替、景気、海外政策の仮説探索 |
| 10 | 日本総研 経済・政策レポート | 17件 | 中長期マクロ、制度、市場構造 |
| 11 | 日本総研 先端技術リサーチ | 20件、最新公開は6月24日 | AI、宇宙、自動運転等のテーマ探索 |
| 12 | JPXマーケットニュース | 13件 | 上場、値幅、監理・整理、上場廃止、指数イベント |
| 13 | JPX株式売買停止情報 | 2件 | 売買停止の要確認アラート |
| 14 | JPX先物・OP売買停止情報 | 0件 | 現時点では利用可と判定しない |
| 15 | 日本銀行 | 0件 | 現時点では日銀一次情報の経路とみなさない |
| 16 | Investing.com 外為ニュース | 0件 | 現時点では利用可と判定しない |
| 17 | Investing.com 経済指標ニュース | 0件 | 現時点では利用可と判定しない |

既存のReuters日本・経済、Yahoo各カテゴリ、ITmedia NEWSも有効です。Reuters経済は実測サンプルで公開から取得まで中央値約7分で、引き続き場中文脈の中心です。一方、新規ソースは同時刻の一括バックフィル痕跡が強く、定常時の取得遅延はまだ評価できません。

## 4. 鮮度契約

すべてのSKILLで、次の時計を別々に保持します。

| 時計 | 意味 | 誤用してはいけない例 |
|---|---|---|
| `as_of` | 分析時刻 | 全データの時点とみなす |
| `upstream_fetched` / `collected_at` | スナップショット取得時刻 | 最終約定時刻とみなす |
| `CurrentPriceTime` | 最終約定時刻 | 板自体の取得時刻とみなす |
| `Buy1` / `Sell1` | 最良買い・最良売り気配 | ゼロや片側板を取引可能とみなす |
| 日足`Date` | 公式取引日 | 夜間の現在値とみなす |
| 信用・空売り基準日 | 週次または公表対象日 | 当日リアルタイム需給とみなす |
| `published_at` | ニュース・開示の公表時刻 | SKILLが利用可能になった時刻とみなす |
| `fetched_at` | NewsStoreへの取得時刻 | 公表時刻とみなす |

### J-Quantsの扱い

J-Quantsは公式性と全銘柄横断に強い一方、更新周期を必ず表示します。下表は[公式の更新タイミング](https://jpx.gitbook.io/j-quants-pro-ja/data-update)を再確認したものですが、公式にも時刻は確約ではなく、通知なく変更され得ると明記されています。

| データ | 主な周期・更新目安 | SKILLでの扱い |
|---|---|---|
| 株価四本値 | 日次、16:30頃 | 引け後の公式日足。場中現在値にしない |
| 上場銘柄一覧 | 日次、17:30頃・翌営業日8:00頃 | 商品区分、市場、コード正規化 |
| 財務情報 | 日次、18:00頃速報・24:30頃確報 | 開示日時と速報・確報を確認 |
| 信用週末残高 | 週次、第2営業日16:30頃 | 基準週末日を明記 |
| 投資部門別 | 週次、第4営業日18:00頃 | 対象週と公表日を明記 |
| 空売り残高報告 | 日次、17:30～19:00頃 | 計算日と公表日を明記 |
| OP日足・建玉 | 日次、建玉は21:15頃 | 過去日次として利用 |

更新予定時刻は保証ではありません。必ず返却された対象日を確認します。`collected_at`が現在でも、古い基準日のデータを「現在」と呼びません。

### リアルタイム系の判定

先物・板は次を分けて評価します。

1. `upstream_fetched`が実行直近か。
2. `Buy1.Price > 0`か。
3. `Sell1.Price > 0`か。
4. `Sell1.Price >= Buy1.Price`か。
5. スプレッドと数量が異常でないか。
6. 最終約定時刻が極端に古くないか。
7. 両脚の取得時刻差と限月が一致するか。

最終約定が5分超なら警告、10分超または無出来高なら実行可能性を低評価する案が妥当です。ただし、最終約定が古いだけで、直近取得した両側板まで古いとは断定しません。

## 5. 市場データの実測

### 5.1 ランキング

`kabus_ranking`で値上がり率、値下がり率、出来高急増、33業種、信用ランキングを取得できました。`Type=2, ExchangeDivision=ALL`の値下がり率は50件で、4419 Finatext Holdingsが1,269円、-23.09%、15:30でした。

制約は次のとおりです。

- 対象日が返らず、価格時刻は`15:30`のように日付なしです。
- 8月14日未明に取得した内容は8月13日終値でした。
- `source_at:null`、`is_stale:null`でした。
- 平日07:53頃から09:00過ぎまでは株価・業種ランキングが空になる場合があります。
- 信用ランキングは週次です。
- 出来高・売買代金は表示単位をJ-Quantsと照合してから計算に使います。例として194Aは224.8対224,800株、7011は23,526.5対23,526,500株でした。

したがって、ランキングは候補抽出と順位確認に使い、流動性・騰落率の確定計算は日付付きJ-Quantsへ接続します。場中は225225.jp等の日付・更新時刻も併用します。

### 5.2 先物板とNT

登録済み銘柄を副作用のない`future_registrations`と`symbol_market_data`で取得した実測です。

| 商品 | 限月 | 現在値 | 価格時刻 | 取得時の最終約定経過 |
|---|---|---:|---|---:|
| 日経225mini | 2026年9月 | 69,430 | 02:32:05 | 約11秒 |
| ミニTOPIX | 2026年9月 | 4,216.00 | 02:31:09 | 約67秒 |
| 日経225micro | 2026年9月 | 69,430 | 02:32:11 | 約5秒 |

`nt_pair_symbol_resolver`は2026年9月限について、ミニTOPIX `161090006`、日経225mini `161090019`、日経225micro `161090023`を返し、同一限月を検証できました。resolverはGETであり、登録状態を変更しません。

既定では登録済み一覧から同じ明示限月の両脚を選びます。該当脚が一覧にない場合もresolverでコードを安全に確認できます。ただし、resolver成功は登録済み板の存在を保証しないため、任意板を取得できなければ具体的枚数・入口を保留します。

板フィールドは一般的な命名感覚と逆です。

```text
Buy1  = 最良買い気配
Sell1 = 最良売り気配
AskPrice = Buy1と一致
BidPrice = Sell1と一致
```

必ず`Buy1`と`Sell1`を正とします。

```text
NTロング:
  日経買い   = 日経Sell1
  TOPIX売り  = TOPIX Buy1

NTショート:
  日経売り   = 日経Buy1
  TOPIX買い  = TOPIX Sell1
```

`kabus_symbol_info`の先物`TradingUnit=1`は契約枚数単位で、指数乗数ではありません。[日経225miniは指数×100円](https://www.jpx.co.jp/derivatives/products/domestic/225mini/index.html)、[日経225microは指数×10円](https://www.jpx.co.jp/rules-participants/public-comment/detail/d8/nlsgeu000006bkfq-att/%28Annex1%29microFutures_J.pdf)、[ミニTOPIXは指数×1,000円](https://www.jpx.co.jp/derivatives/products/domestic/mini-topix-futures/01.html)という乗数を、実行時にもJPX公式仕様で確認して枚数計算します。

### 5.3 任意現物板、規制、銘柄情報

4419の任意板では、終値1,269円、前日終値1,650円、安値1,252円、出来高3,106,400株、売買代金4,042,085,100円、VWAP 1,301.212円、最良買い1,268円、最良売り1,271円を取得できました。市場休場中だったため価格時刻は8月13日15:30、取得時の`age_seconds`は約39,510秒でしたが、`is_stale:null`でした。

銘柄情報では決算期、時価総額、値幅上限・下限、売買単位等を取得できます。ただし単位をそのまま信用してはいけません。7011の`TotalStocks`は約337万という表示に対し、実発行株式は約33.7億株に相当し、千株単位と考えられます。1株価値や希薄化率へ使う前にJ-Quants・EDINETで検算します。

194Aはランキングには存在しましたが、板・銘柄情報が`INVALID_ARGUMENT`でした。板取得不能を銘柄不存在とせず、J-Quants公式終値へフォールバックします。

規制と信用プレミアムはGETであり、登録容量へ影響しません。復活ランキングでは除外ゲートとして候補全件に使えます。板や財務の深掘りは処理量を抑えるため最終候補へ絞ります。

### 5.4 オプション

KabusControllerのOP登録範囲は、単純な「取得時点ATM」ではありません。次のセッションに対応する日経225先物の直前セッション終値を基準に構成されます。

| 自動登録を行う局面 | 基準価格 | 実装上の価格区分・市場 |
|---|---|---|
| ナイトセッション開始前 | 直前デイセッション終値 | `day-close`、市場23 |
| デイセッション開始前 | 直前ナイトセッション終値 | `night-close`、市場24 |
| 障害復旧等でセッション途中に起動 | 起動時点の日経225先物値 | `current`、日通し市場2 |

実装はJST 06:00～08:44を`night-close`、15:45～16:59を`day-close`、それ以外を`current`として扱います。通常の切替時刻またはAPI認証成功時に、基準となるラージ日経225先物の`CurrentPrice`を取得し、欠損時だけ`CalcPrice`へフォールバックします。ここでいう前終値は板応答の`PreviousClose`フィールドではなく、日中市場23または夜間市場24に残る最終`CurrentPrice`です。

基準価格を最も近い125円単位へ補正し、`TRADE.OPTION.PriceRange`の上下を125円刻みで展開して、各権利行使価格のCallとPutを両方対象にします。現行設定例の`PriceRange=1000`なら、補正後の基準価格を中心とした上下1,000円が候補です。先物を含む登録上限50件を超える場合は、基準価格から遠いOPから省かれます。

初回監査時の登録状態は、2026年8月13日16:45 JSTの`scheduled-switch`、`day-close`、基準価格68,260円、価格時刻15:45:01 JST、125円補正後の中心68,250円、範囲67,250～69,250円でした。先物11件とOP34件の合計45件でしたが、設定限月はSQを迎えた`202608`のままでした。

単体resolverの`strike_price=0`で2026年9月限を解決したところ67,500円が返りましたが、同時刻の日経225miniは約69,425円でした。この`0`解決はKabusControllerの前終値基準のOP登録ロジックとは別機能です。SKILLは`strike_price=0`を現在ATMとみなさず、Controllerが実際に登録した範囲と登録時の基準価格を使います。

### 限月更新後の再確認

SQ日の設定更新後、2026年8月14日03:08～03:11 JSTにGETだけで再確認しました。

| 項目 | 再確認結果 |
|---|---|
| 登録処理 | `state=succeeded`、`trigger=authentication`、エラー・スキップ0件 |
| 登録完了 | 03:09:33 JST |
| 基準 | `priceMode=current`、69,350円、03:08:53 JST |
| 125円補正中心 | 69,375円 |
| 登録範囲 | 68,375～70,375円 |
| 登録限月・件数 | `NK225op 202609`、17権利行使価格×Call・Put＝34契約 |
| 全登録数 | 先物11件＋OP34件＝45件 |
| チェーン | `coverage.complete=true`、`missing=[]` |
| 次回切替予定 | 2026年8月14日16:45 JST |

`option_market_data`では34契約すべてに`Buy1`と`Sell1`の両側板があり、`CalcPrice`とIVも34件すべて取得できました。現在値、最終約定時刻、正の出来高があったのは8件です。残る26件は未約定でも両側板が存在するため、`CurrentPrice=null`を「板なし」と判定してはいけません。建玉はチェーンから取得できません。

この結果から、初回の空データはSQ時の設定限月更新漏れによる一時状態であり、OPリアルタイムJSON取得機能の不具合ではないと訂正します。なお、確認したリポジトリの`conf/trade.toml`は旧限月のままで、稼働サーバーは別配置の更新済み設定を使用していました。SKILLは設定ファイルを推測せず、毎回、実登録一覧と登録ステータスを正とします。

OPでは、次の項目は同義ではありません。

```text
登録カバレッジ完全    ≠ 両側板が存在する
CurrentPriceがnull    ≠ 板が存在しない
CurrentPriceTime      ≠ 板の更新時刻
IVが非null            ≠ 現在板から逆算した市場IV
CalcPriceが非null     ≠ 約定可能価格
resolver成功          ≠ Controllerから板取得可能
```

オプションを予測へ使う最低条件は、取引可能な同一限月、取引最終日前、現在先物が登録範囲内、直近の`upstream_fetched`、コール・プット双方の正の`Buy1`・`Sell1`、正常な売買スプレッド、必要権利行使価格の欠損なしです。最終約定時刻と出来高は流動性評価へ使いますが、未約定だけを理由にライブ板を失効させません。トップレベルの`AskPrice`・`BidPrice`は向きが逆なので、OPでも`Buy1`を最良買い、`Sell1`を最良売りとして使います。

加えて、登録時の`referencePrice`、`referencePriceAt`、`priceMode`、`optionMinimumPrice`、`optionMaximumPrice`を確認し、どのセッション向けに作られた窓かを表示します。生のIVは計算根拠と時点を検証できない限り「現在板から逆算したIV」と呼ばず、板の中値等から再計算できる場合だけライブIVとして扱います。建玉PCRはこのチェーンから作らず、J-Quantsの前営業日建玉を日次データとして別表示します。

### 5.5 FXと容量

許可値11ペアを実測し、成功したのはUSD/JPYだけでした。例ではAsk 159.525、Bid 159.523、`Spread=0.2`でした。実価格差は0.002円なので、価格スプレッドは`AskPrice - BidPrice`で再計算し、`Spread`を同じ円単位として使いません。`Time`に日付がないため、225225.jp等の日付付き為替と照合します。

API容量は上限50、Controller既知45、残枠上限5でした。これはControllerが自動登録した先物・OP窓の充足状況を診断する補助情報で、GET実行の可否を制限する値ではありません。`kabus_api_soft_limits`の先物100、mini 200、micro 200、OP 100等は1注文当たりの上限であり、証拠金、推奨枚数、登録残枠ではありません。

## 6. ニュースの実測と使い分け

### 6.1 取得品質

- JPXマーケットニュースは制限値幅拡大、監理・整理、上場廃止、新規上場、指数関連の一次アラートとして有用です。
- JPX株式売買停止の保存見出しには会社名・4桁コードが残りましたが、本文再取得時にはライブページの「現在、該当する情報はありません」へ置き換わりました。過去停止理由の証拠には使えません。
- 大和総研は本文を取得できますが、ナビ、関連記事、著作権文言の混入が多く、本文検索で偽陽性が出ます。
- 日本総研経済は記事によって日付と著者しか取得できず、分析本文の証拠には使えません。
- 日本総研先端技術は概要・目次を得られる一方、関連記事の語でも検索に当たります。
- ITmediaは同じURLの記事が複数返る例や、ページ共通部・関連記事によるテーマ偽陽性があります。
- 8月13日下落率上位20銘柄を正式名と4桁コードで検索した結果、検証できた個別記事は0件でした。
- 4419の決算と6141の海外募集新株式発行もニュース検索では0件でしたが、EDINET DBでは確認できました。
- `適時開示`、`TDnet`、`決算短信`、`新株式`の網羅フィードは確認できませんでした。JPXマーケットニュースはTDnetではありません。

### 6.2 検索規則

検索は最大5語のAND条件なので、次を別々に検索します。

- 正式会社名
- 「株式会社」を除いた名称
- 略称
- 英文名
- 4桁または英数字の表示コード
- 全角・半角を含む指数表記

5桁J-Quantsコードをニュース検索へそのまま渡しません。`TOPIX`と`ＴＯＰＩＸ`、`HD`と`ＨＤ`などは別検索が必要です。一般語と衝突する社名は、見出しに会社固有語または4桁コードがあるか、記事本体の局所文脈で社名とコードの両方が一致した場合だけ採用します。

重複排除の基本キーは`source_id + external_id`です。同じキーの複数改訂は`updated_at`、`revision_id`、`fetched_at`が最新のものを残します。JPXは異なるイベントが同じ一覧URLを共有するため、`canonical_url`だけで消してはいけません。異ソースの同一事象は削除せず1つのイベント群にまとめ、一次情報を主、報道を市場解釈として保持します。

### 6.3 各実行時のソース健全性

各SKILLは、少なくとも次を確認します。

| 状態 | 解釈 |
|---|---|
| `enabled=true`かつ最近の記事あり | 検索対象として利用 |
| `enabled=true`だが記事0件 | 稼働未確認。材料なしとは判定しない |
| 公開から取得まで長時間 | バックフィルまたは遅延。即時触媒にしない |
| 本文が短い・可変ページ | 見出しアラートに限定し、別の一次資料で確認 |

## 7. コード接続と数値整合

Kabuの省略名をそのまま会社検索へ渡さず、証券コードを主キーにします。実測した194Aの接続は次のとおりです。

```text
Kabu             194A / ウルフハンド
J-Quants         194A0 / WOLVES HAND
EDINET DB        E39640 / secCode 194A0
政策DB           sec_code 194A0
Yahoo Finance    194A.T
```

EDINETで「ウルフハンド」は0件でしたが、コードでは正しく解決できました。各候補に、表示用コード、J-Quantsコード、EDINETコード、正式社名、法人番号、市場区分、外部ティッカーを持つ正規化レコードを作ります。5桁コードを機械的な末尾0付与だけで生成しません。

### 現在値と倍率の期間整合

194Aでは次の差が確認できました。

| 項目 | 値 |
|---|---:|
| 2026年8月13日終値 | 1,900円 |
| FY2026実績EPS | 124.88円 |
| FY2027会社予想EPS | 132.90円 |
| 現在値で再計算した実績PER | 約15.21倍 |
| 現在値で再計算した会社予想PER | 約14.30倍 |
| EDINET会社情報に保存されたPER | 9.36倍 |
| Yahooに保存されたPER | 20.17倍、EPS 94.2円基準 |

EDINET会社情報とYahooの倍率は評価時点・EPS期間が異なっていました。バリュエーションは必ず次の順で再計算します。

1. 現在値または最終公式終値の時刻を確定する。
2. EPSの決算期、実績・会社予想・市場予想、株式分割調整を確定する。
3. `評価価格 ÷ 同じ評価期間のEPS`で再計算する。
4. 実績、会社予想、コンセンサスを別列にする。
5. EPS不整合が解決しなければ、点の価格目標を停止して感応度レンジだけを示す。

## 8. 4つのSKILL設計の改訂

### 8.1 日経225・TOPIX・NT予測

既定経路はMCPが公開する情報GETだけを使います。これらのGETは銘柄登録状態を変更しません。

1. `datalist`を確認する。
2. 取引カレンダーとJSTから現物・先物の市場状態を判定する。
3. `nt_pair_symbol_resolver`で明示限月のミニTOPIXと日経miniまたはmicroを解決する。
4. 両脚の限月、取引最終日、ロール状態を検証する。
5. 登録済みなら`symbol_market_data`、それ以外は`arbitrary_board_snapshot`で各脚の板を出力直前に取得する。
6. 225225.jpで現物、夜間先物、NT倍率、為替、海外、33業種、日経寄与度を取得する。
7. J-Quantsの日足、投資部門別、信用、OP日足は基準日付きの遅行データとして使う。OPは登録限月、取引最終日、登録時の基準価格・セッションを先に検証する。
8. Reutersを場中文脈、研究機関をマクロ仮説、JPXを取引所イベントの確認に使う。
9. 単純ロング・ショートとNTの期待値を比較し、優位がない場合は「見送り」を出す。
10. NT枚数は同一時点の板、JPX公式乗数、許容名目差から再計算する。

resolverと任意板もGETなので、未登録銘柄の確認に使用できます。オプションは実登録限月が取引可能で、登録窓が現在先物を十分に覆い、対象ストライクのCall・Putに両側板が揃う場合だけ方向スコアへ加えます。`CurrentPriceTime`は最終約定時刻であり、板鮮度の時刻ではありません。

### 8.2 復活可能性ランキング

処理順を次に改めます。

1. `kabus_ranking`と225225.jpで速報候補を取得する。
2. J-Quantsの当日全普通株から騰落率を再計算し、対象日と母集団を確定する。
3. コードを正規化する。
4. JPXニュースで停止、監理・整理、上場廃止、制限値幅拡大を先に確認する。
5. EDINETの決算、希薄化、業績修正、企業行動で急落原因を一次確認する。
6. ニュースで市場解釈と外部環境を補う。ニュース0件は「原因未確認」とする。
7. 政策DBは直近かつ重要な直接リンクだけを補助材料にする。
8. 復活根拠指数と売買成立指数を分離する。
9. 候補全件の規制を確認し、最終候補について任意板と信用プレミアムを確認する。
10. 円価格の入口、損切り、第1・第2利確、時間撤退、材料撤退を提示する。

低位・薄商い、規制、売買停止、原因不明、未確定の大規模希薄化、決算直前は、点数を付ける前に除外または保留します。急落当日の成行逆張りを既定にせず、反転確認後の条件付き入口とします。

### 8.3 市場拡大・中長期成長株

当日ランキングは成長点へ加えず、注目度・過熱・入口タイミングの別レイヤーにします。

評価は次の層に分けます。

1. 市場規模・需要量・設備計画の独立した一次証拠
2. EDINETの売上成長、利益率、CF、受注残、セグメント、KPI
3. 競争優位、顧客集中、希薄化、ガバナンス
4. 政策DBの直接的な企業リンク、認定、調達、補助
5. 現在値と同一期間EPSから再計算したバリュエーション
6. 板、流動性、規制、値幅による実行タイミング

研究機関ニュースはテーマ発見に使い、本文中の数値、期間、出典を確認できない限り市場拡大点へ加えません。政策DBが0件でも減点せず、「観測された直接的政策リンクなし」と表示します。

出口は固定目標だけでなく、業績前提の破綻、KPI鈍化、受注残減少、利益率低下、過大な倍率、時間経過を組み合わせます。

### 8.4 特定銘柄予測

現在値の優先順位は次のとおりです。

1. 日付・市場状態を検証したKabu板
2. J-Quantsの最終公式終値
3. 遅延と銘柄種別を確認した外部価格

Kabu板が`INVALID_ARGUMENT`ならJ-Quantsへフォールバックし、休場中は「現在値」ではなく「最終取引日終値」と表示します。EDINETで同日決算・希薄化・自社株・M&A等を確認し、ニュースは市場解釈を補います。

予測は1・5・20営業日等の期間別に、弱気・基本・強気の価格帯、根拠、無効化条件を示します。シナリオのEPSとPERは同一年度に揃えます。規制、売買停止、ストップ高・安、信用プレミアムは企業価値スコアではなく出口・執行条件へ反映します。

## 9. 全SKILL共通の状態変更境界

- 市場データ収集MCPが公開する情報GETは読取専用として使用します。
- 応答に`read_only:false`、`may_register_symbol:true`が残っていても、現行実装では登録状態を変更しない既知のメタデータ不整合として扱います。
- `/register`、`/unregister`、`/unregister/all`は呼びません。
- 注文、取消、口座・建玉操作も行いません。
- `kabus_api_capacity`は自動登録された先物・OP窓の構成確認に使用し、情報GETの実行制限には使いません。
- 必要な板、対象日、取引可能限月が不足すれば、価格案や枚数案を保留します。

## 10. 市場データ収集側に残る改善候補

追加されたデータセットは有用ですが、完成SKILLを安定運用するには次が残ります。

### 優先度A

1. ランキングへ対象取引日、日付付き`source_at`、単位を追加する。
2. 情報GETの`MayRegisterSymbol`をfalse、`read_only`をtrueへ訂正し、誤った`side_effect_notice`を削除する。
3. `GET /kabus/status`の`data.tradeRegistration`を返す読取専用データセット`kabus_trade_registration_status`を追加するか、`option_chain_snapshot`へ次を付加する。これがOP登録窓を正確に監査するための新たな必要データです。
   - `state`、`active`、`errors`
   - `trigger`
   - `startedAt`
   - `referencePrice`
   - `referencePriceAt`
   - `priceMode`
   - `optionMinimumPrice`
   - `optionMaximumPrice`
   - `completedAt`
   - `nextScheduledAt`
   - 先物・OP別の登録件数とスキップ件数
4. `option_chain_snapshot`へ、`Buy1`・`Sell1`に基づく`two_sided_quote_availability`と、板スナップショット取得時刻`quote_snapshot_at`を追加する。現行の`quote_time_availability`は`CurrentPriceTime`、すなわち最終約定時刻の有無であり、両側板の取得可否を表していない。
5. `is_stale`と`market_state`を市場カレンダー・セッション別に確定する。
6. `AskPrice`・`BidPrice`の向きを修正するか、正規化済み`best_bid`・`best_ask`を追加する。
7. FXの許可ペアと実装を一致させ、日付付き時刻と価格単位の`spread_price`を返す。

### 優先度B

8. OP resolverの`strike_price=0`を現在ATMと誤認しない名称・説明へ変更する。
9. resolver成功、Controller登録範囲内、板取得成功を別ステータスで返す。
10. 取引最終日を過ぎた固定限月と、現在先物が登録範囲外・範囲端にある状態を警告する。
11. OP設定限月の自動ロール有無を返し、現行の固定限月を明示する。
12. `TotalStocks`、ランキング出来高・売買代金等に機械可読の単位を付ける。

## 11. 次に作るフォルダ構成

再監査を反映した候補名は次のとおりです。

```text
pnd-forecast-japan-index-nt/
pnd-rank-japan-stock-rebounds-v2/
pnd-rank-japan-growth-markets/
pnd-forecast-japan-stock/
```

各`SKILL.md`は取得順、判定ゲート、失効条件、最終出力だけを簡潔に持たせ、詳細は`references/`へ分離します。共通化する内容は、データ契約と鮮度、コード正規化、ニュース検索・重複排除、数値整合、出力上の不確実性です。

直接サーバーURL、`X-API-KEY`、注文・登録管理はSKILLへ書きません。ライブデータ取得はMCP、SKILLは取得の順序・検証・推論・出口設計を担当します。
